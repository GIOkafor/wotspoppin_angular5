import { EventInfoFilterPipe } from './event-info-filter.pipe';

describe('EventInfoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EventInfoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
