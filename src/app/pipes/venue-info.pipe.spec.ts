import { VenueInfoPipe } from './venue-info.pipe';

describe('VenueInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new VenueInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
